import logger from './logger';

export default {
  /** Signs a token
   * @param {String} token
   * @returns {String} signed token
   * @todo sign the token
   */
  signToken: (token) => {
    return token;
  },

  /** Verifies a token
   * @param {String} token
   * @returns {String} approved token
   * @todo verify the token
   */
  verifyToken: (token) => {
    return token;
  },

  /** Hash token
   * @param {String} token
   * @returns {String} hashed token
   * @todo hash the token
   */
  hashToken: (token) => {
    return token;
  },
}
